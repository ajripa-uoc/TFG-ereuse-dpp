apiVersion: v1
kind: Service
metadata:
  name: {{ include "id-index-api.fullname" . }}
  labels:
    {{- include "id-index-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "id-index-api.selectorLabels" . | nindent 4 }}
