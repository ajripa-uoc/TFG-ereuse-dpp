apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ereuse-dpp.fullname" . }}-test-connection"
  labels:
    {{- include "ereuse-dpp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ereuse-dpp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
